import { AdminLoginForm } from "@widgets/AdminLoginForm";
import { Cursor } from "@shared/ui/Cursor";

export const Login = () => {
  return (
    <section>
      <Cursor />
      <AdminLoginForm />
    </section>
  );
};
